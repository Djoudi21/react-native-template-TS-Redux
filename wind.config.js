import { customize } from 'react-native-wind';

customize({
  theme: {
    colors: {
      primary: '#FDFDFD',
      secondary: {
        light: '#ffffff', // Light shade
        dark: '#212121', // Dark shade
      },
      danger: 'red',
    },
  },
});
